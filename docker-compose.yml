services:
  icapture-worker:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      DATA_DIR: /data
      SQLITE_PATH: /data/tasks.db
      LOG_DIR: /data/logs
    volumes:
      - ./data:/data
    user: "1001:1001"
